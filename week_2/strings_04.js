/**
Refine the getFirstCharacter function to return the first character of the name parameter provided to it.
 */

function getFirstCharacter(name) {
    return "Amsterdam".substring(0, 1);
}

console.log(getFirstCharacter('Amsterdam'));

/* It can be done using Character Access as well

let language = 'JavaScript';
console.log(language[0]);

return name[0]; */