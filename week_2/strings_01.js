/**
Refine the function getCharCount so that it returns the count of characters present in the str parameter it receives.
 */

//let getCharCount = "Samuel Sam";

function getCharCount(str) {
    return str.length
}

console.log(getCharCount('Sam')); 
console.log(getCharCount('Alex 123')); 
console.log(getCharCount('Charley is here'));



